<nav id="navbar" class="navbar navbar-expand bg-light shadow-sm">
    {% block navbar %}
        <div class="container">
            <a class="navbar-brand" href="{{ url_for('main') }}">404 Not-Found</a>
            <button data-bs-toggle="collapse" class="navbar-toggler" data-bs-target="#navcol-1"></button>
            <div class="collapse navbar-collapse" id="navcol-1">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ url_for('toetsvragen_view') }}">Vragen</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url_for('prompts_view') }}">Prompt</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Redacteuren</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {% if 'user_id' not in session %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url_for('login_route') }}">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url_for('sign_up_route') }}">Sign Up</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <span class="nav-link">Welcome, {{ session['display_name'] }}</span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ url_for('logout_route') }}">Logout</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    {% endblock %}
</nav>